# MDD Audit Notes — 2026-03-26

## Files Analyzed: 25 source files + 2 config files

---

### [01] Core & Routing (App.jsx, main.jsx)
**Files read:** App.jsx (117 lines), main.jsx (66 lines)
**Findings:**
- ✅ 404 route now implemented (NotFound component)
- ✅ Clean, well-structured routing
- ⚠️ MEDIUM: No `/films/:slug` route exists anymore but CLAUDE.md still lists it — stale documentation
- ℹ️ INFO: SmoothScrollContext is no longer in main.jsx, now in Layout.jsx — good separation
**Test coverage:** 0 / N/A
**Doc accuracy:** CLAUDE.md Architecture section is stale (lists deleted files)

### [02] Hero Section (HeroSection.jsx)
**Files read:** HeroSection.jsx (76 lines)
**Findings:**
- ✅ Massively simplified from 523 → 76 lines — excellent refactor
- ✅ Uses svh units correctly, clean mobile/desktop split
- ⚠️ MEDIUM: Mobile hero renders nav links (`navCenter.map`) using `<a href>` instead of React Router `<Link>` — causes full page reload on navigation
- ⚠️ LOW: Two `<h1>` elements (firstName + lastName) in mobile — should be single `<h1>` for SEO
**Test coverage:** 0 / N/A

### [03] Floating Gallery Hero (FloatingGalleryHero.jsx)
**Files read:** FloatingGalleryHero.jsx (41 lines)
**Findings:**
- ✅ Massively simplified from 717 → 41 lines — replaced complex rAF marquee with static image
- ⚠️ MEDIUM: coverImage references `/src/assets/` path which won't work in production build (Vite doesn't serve from /src/ at runtime)
**Test coverage:** 0 / N/A

### [04] Pages (Films, Photos, PhotoProject, About, Contact)
**Files read:** Films.jsx (107), Photos.jsx (118), PhotoProject.jsx (87), About.jsx (344), Contact.jsx (39)
**Findings:**
- ✅ Films.jsx — Clean refactor, film detail replaced with modal pattern (FilmModal)
- ✅ Contact.jsx — Massively simplified from 486 → 39 lines, removed broken form, now just links
- ⚠️ HIGH: Films.jsx ShowreelOverlay sets `document.body.style.overflow = 'hidden'` on open and `''` on close — conflicts with SmoothScrollContext which also sets overflow. Should save/restore previous value (like Lightbox does)
- ⚠️ MEDIUM: Films.jsx ShowreelOverlay and FilmModal both implement identical overlay pattern (close button, escape key, loading spinner, iframe) — should extract shared component
- ⚠️ LOW: About.jsx at 344 lines still exceeds 300-line quality gate, but it's purely content — acceptable
**Test coverage:** 0 / N/A

### [05] Layout Components (Navigation, Layout, Footer)
**Files read:** Navigation.jsx (175), Layout.jsx (148), Footer.jsx (120)
**Findings:**
- ✅ Clean, well-structured components
- ⚠️ LOW: Navigation has redundant `role="navigation"` on `<nav>` element (nav already has implicit navigation role)
**Test coverage:** 0 / N/A

### [06] Film Components (FilmCard, FeaturedFilmCard, FilmModal, VideoPlayer)
**Files read:** FilmCard.jsx (100), FeaturedFilmCard.jsx (126), FilmModal.jsx (65), VideoPlayer.jsx (142)
**Findings:**
- ✅ FilmCard simplified from 261 → 100 lines, percentage-based positioning
- ⚠️ HIGH: FilmModal sets `document.body.style.overflow = ''` on close — same overflow conflict as ShowreelOverlay (should save/restore)
- ⚠️ MEDIUM: FilmCard has no responsive handling — `aspect-[2/1] max-h-[650px]` with absolute positioned images will break on small screens (no mobile path)
- ⚠️ LOW: VideoPlayer.jsx appears unused now (films use FilmModal with direct iframe, not VideoPlayer component)
- ℹ️ INFO: FeaturedFilmCard uses inline `fontFamily: 'Helvetica Now Display'` instead of Tailwind `font-header` class — inconsistent
**Test coverage:** 0 / N/A

### [07] Photo Components (ProjectCard, PhotoGrid)
**Files read:** ProjectCard.jsx (166), PhotoGrid.jsx (138)
**Findings:**
- ✅ Clean, well-structured components with good accessibility
**Test coverage:** 0 / N/A

### [08] Data Files (siteConfig, films, photoProjects)
**Files read:** siteConfig.js (216), films.js (186), photoProjects.js (251)
**Findings:**
- ✅ Formspree placeholder removed — Contact now uses direct email link instead
- 🔴 CRITICAL: photoProjects.js references `/src/assets/photos/...` paths for all images — these paths won't resolve in production builds. Vite serves from `public/` at runtime, not `src/assets/`
- ⚠️ MEDIUM: films.js comment says "Add the thumbnail image to /src/assets/films/" but actual paths use `/posters/` and `/videos/` (public dir) — stale comment
- ℹ️ INFO: `galleryLayout.js` was deleted — referenced in previous audit as 72 lines
**Test coverage:** 0 / N/A

### [09] Context & UI Components (SmoothScrollContext, PersistentHeroText, Lightbox, Divider)
**Files read:** SmoothScrollContext.jsx (415), PersistentHeroText.jsx (118), Lightbox.jsx (307), Divider.jsx (55)
**Findings:**
- ⚠️ HIGH: SmoothScrollContext still exceeds 300-line quality gate at 415 lines
- ✅ Lightbox correctly saves/restores `previousOverflow` — good pattern from last audit fix
- ⚠️ MEDIUM: SmoothScrollContext touch handler calls `e.preventDefault()` on touchmove — this blocks native scrolling on mobile but the provider claims to fall back to native scroll on mobile. Dead code path?
- ⚠️ LOW: PersistentHeroText splits tagline by spaces (`tagline.split(' ')`) — assumes exactly 3 words. If tagline changes, layout breaks
**Test coverage:** 0 / N/A

### [10] Styles & Config (index.css, tailwind.config.js)
**Files read:** index.css (516), tailwind.config.js (123)
**Findings:**
- ✅ Clean design system with CSS variables
- ⚠️ MEDIUM: CLAUDE.md says fonts are `font-header` (Steps-Mono), `font-body` (American Typewriter) but actual fonts are Helvetica Now Display and Helvetica Now Text — documentation is wrong
- ⚠️ LOW: `Helvetica Now Micro` fonts in public/fonts (per git status) but no @font-face declarations for them — unused font files
- ℹ️ INFO: Deleted files from git status: IntroSection.jsx, galleryLayout.js, useParallaxLayers.js, useStaggeredText.js, FilmDetail.jsx — all cleaned up from simplification
**Test coverage:** 0 / N/A

### [11] Quality Gate Summary
| File | Lines | Limit | Status |
|------|-------|-------|--------|
| SmoothScrollContext.jsx | 415 | 300 | ❌ Over by 115 |
| About.jsx | 344 | 300 | ⚠️ Over by 44 (content-only) |
| Lightbox.jsx | 307 | 300 | ⚠️ Barely over (7 lines, mostly comments) |

### [12] Deleted Files (cleaned up)
- ✅ IntroSection.jsx — removed
- ✅ galleryLayout.js — removed
- ✅ useParallaxLayers.js — removed
- ✅ useStaggeredText.js — removed
- ✅ FilmDetail.jsx — removed (replaced by FilmModal)

# MDD Audit Notes — 2026-05-20

Audit scope: all 7 docs. Previous audit: 2026-05-05. Heavy CMS work happened
between (Stages 1 + 3 + 4 + Deploy tool, PRs #5/#6/#7/#8/#9/#10/#11).

---

### [01] Photos Hero — Flashing Photo Slideshow
**Files read:** `src/components/photos/FloatingGalleryHero.jsx`, `src/data/heroPhotos.js`, `src/components/ui/HeroSection.jsx`
**Findings:**
- 🟡 STALE `source_files`: doc lists `src/data/heroPhotos.js` as the source. **Reality:** FloatingGalleryHero now imports `heroPhotos` from `src/sanity/loader.js`. The legacy data file is the fallback only (loader returns it when `cms.json.heroPhotos.photoUrls` is empty).
- 🟡 Doc body never mentions that the slideshow is now CMS-driven (Stage 3). Reader following the doc would not realise the photos can be edited in `/admin → 🖼️ Hero photos`.
- 🟢 Behavioural sections (shuffle, preload-ahead, landscape-skip on mobile, prefersReducedMotion) still match the code.
- ⚪ Known issue #1 (silently-frozen frame after 20 retries) still in code; still open.
- ⚪ Known issue #2 (prefersReducedMotion read once) still in code; still open.
**Tests:** none reference this component.

### [02] BABA Portfolio Design System
**Files read:** `src/styles/index.css`, `tailwind.config.js`, `src/data/siteConfig.js`
**Findings:**
- 🟡 STALE: doc lists `src/data/siteConfig.js` as a design-system source. **Reality:** `siteConfig.js` is now a compatibility shim — most fields stream from `src/sanity/loader.js`; only a few (`tagline`, `shortBio`, `title`, `contact.email/phone/location`, navigation labels) remain hardcoded. The doc never mentions the CMS flow.
- 🟡 Missing reference to `src/utils/fluidScale.js` — the new shared `clamp()` helper that drives nav-link and hero-overlay sizing across the entire site is a design-system concern but absent.
- 🟢 CSS variable / font / Tailwind config sections still accurate.
**Tests:** none.

### [03] Collapsed Film Cards
**Files read:** `src/components/films/CollapsedFilmCard.jsx`, `src/components/films/FeaturedFilmCard.jsx`, `src/pages/Films.jsx`
**Findings:**
- 🟢 source_files all exist and behaviour described matches the code (no CMS migration touched the films pipeline yet — Stage 5 unbuilt).
- 🟡 Films.jsx still imports `films` from `src/data/films.js`. Once Stage 5 lands (films → CMS collection) this doc will need the same treatment as 05/06.
- ⚪ Known issues all still present and acceptable per their notes.
**Tests:** none referenced.

### [04] Featured Card — Scroll-Tied Fade at Viewport Edges
**Files read:** `src/components/films/FeaturedFilmCard.jsx`
**Findings:**
- 🟢 Matches the code. `tests/homepage.spec.js` exists (62 lines).
- ⚪ No known issues.
**Tests:** `tests/homepage.spec.js` ✓

### [05] Featured Photo Cards — Compact Preview ↔ Expanded Gallery (FLIP morph)
**Files read:** `src/components/photos/FeaturedPhotoCard.jsx`, `src/components/photos/PhotoCardPreview.jsx`, `src/components/photos/ExpandedPhotoGallery.jsx`, `src/pages/Photos.jsx`, `src/data/photoProjects.js`, `scripts/scaffold-photo-projects.mjs`
**Findings:**
- 🟡 STALE `source_files`: lists `src/data/photoProjects.js` as the data source. **Reality:** Photos.jsx now imports `photoProjects` + helpers from `src/sanity/loader.js`. The data file is the fallback only.
- 🟡 STALE `source_files`: lists `scripts/scaffold-photo-projects.mjs` — still present in repo but probably obsolete now that content lives in Sanity; not used by any current workflow.
- 🟢 The recently added known-issue entry (mobile-fallback fields preserved via `previewMobilePattern`/`previewMobilePhotoIndices`/`previewMobileBreakpoint`/`previewAspectRatio`/`previewMaxHeight` in `sanity/schemas/photoProject.js`) is accurate — good preventative documentation.
- 🟡 Doc body never mentions Stage 4 schema, drag-to-reorder via `@sanity/orderable-document-list`, or the `orderRank` field. Reader would not know the page order is set in Studio.
**Tests:** `tests/e2e/featured-photo-cards.spec.js` (238 lines) ✓

### [06] Collapsed Photo Cards — Other Projects single-row → gallery shutter
**Files read:** `src/components/photos/CollapsedPhotoCard.jsx`, `src/components/photos/ExpandedPhotoGallery.jsx`, `src/pages/Photos.jsx`, `src/data/photoProjects.js`
**Findings:**
- 🟡 STALE `source_files`: same issue as 05 — `src/data/photoProjects.js` referenced but the real source is `src/sanity/loader.js`.
- 🟢 Single-expand orchestration described is still accurate.
**Tests:** `tests/e2e/featured-photo-cards.spec.js` ✓ (shared with doc 05)

### [07] CMS Foundation + Site Copy Migration (Stage 1 of 6)
**Files read:** all 16 source_files listed
**Findings:**
- 🔴 TITLE WRONG: doc claims "Stage 1 of 6". Stages 1, 3, **and** 4 are completed in code; doc only describes Stage 1. Body of the doc does mention "future stages" but doesn't reflect what's actually built.
- 🔴 INCOMPLETE `source_files`. Missing files that are firmly part of the CMS layer now:
  - `sanity/schemas/heroPhotos.js` (Stage 3 singleton)
  - `sanity/schemas/photoProject.js` (Stage 4 collection)
  - `sanity/tools/DeployTool.jsx` (Studio Deploy tool)
  - `scripts/seed-cms.mjs`, `scripts/watch-cms.mjs`, `scripts/upload-hero-photos.mjs`, `scripts/upload-photo-projects.mjs`, `scripts/fix-photo-project-ranks.mjs`
  - `src/utils/fluidScale.js`
  - `src/components/layout/Navigation.jsx` (now CMS-driven for active-style + link sizes)
  - `src/components/photos/FloatingGalleryHero.jsx` (now reads heroPhotos from loader)
  - `src/pages/Photos.jsx` (now reads photoProjects from loader)
- 🔴 NO MENTION of:
  - `orderRank` + `@sanity/orderable-document-list` drag-to-reorder
  - LexoRank format for the rank field (the fix that finally made drag deterministic)
  - The Vercel deploy hook workflow + the Studio Deploy tool
  - The Sanity webhook setup (GROQ filter, drafts/versions off, the dedupe gotcha, the rate-limit dance)
  - The `cms:fix-photo-project-ranks` + `cms:upload-*` scripts and when to run each
- 🟡 The "Data Model" section only documents siteSettings/heroOverlay/showreel. heroPhotos + photoProject are missing entirely.
- 🟡 No Stage 3/4 known-issues recorded (e.g. zero-padded vs LexoRank migration bug, Vercel deduplication of back-to-back deploy-hook triggers, nested-scroll DnD warning that doesn't actually break anything).
**Tests:** none — no Playwright specs for any of the CMS work (Studio mount, content loader, Deploy tool).

---

## Cross-cutting findings

- 🔴 **No MDD docs for Stages 3 and 4.** Doc 07 covers Stage 1 only. Stage 3 (heroPhotos) and Stage 4 (photoProject collection) ship a non-trivial schema + scripts + Studio integration with **zero documentation**. Should either become two new docs (08-cms-hero-photos, 09-cms-photo-projects) OR be folded into a rewritten doc 07 that covers all CMS stages.
- 🔴 **No tests for any CMS feature.** Test plan in doc 07 listed `tests/e2e/cms-studio-loads.spec.js`, `cms-hero-overlay.spec.js`, `cms-site-copy.spec.js`, `removed-pages-404.spec.js` — none were ever written. Same for the Deploy tool.
- 🟡 **`src/data/siteConfig.js` still has hardcoded fields:** `tagline`, `shortBio`, `title`, `contact.email/phone/location`, and the entire navigation array. The CMS migration was "Stage 1" yet these weren't moved — they should at least be documented as known gaps.
- 🟡 **Scripts dir has 5 CMS scripts** none of which appear in any doc's source_files. They should be referenced + each one's purpose described in a doc.
- 🟡 **`.env.example` requires manual edits** (`SANITY_WRITE_TOKEN`, `VITE_VERCEL_DEPLOY_HOOK_URL`) that aren't documented in any feature doc — only in CLAUDE.md.

## Severity legend
- 🔴 CRITICAL — incorrect or wholly missing documentation for built features. A reader cannot trust the docs.
- 🟡 MEDIUM — partially stale; reader gets the gist but specifics are wrong.
- 🟢 OK — doc matches code.
- ⚪ KNOWN — pre-existing known issue, not a new finding.

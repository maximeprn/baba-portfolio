# MDD Audit Notes — 2026-06-12 (full project)

Scope: all 17 docs. Method: 5 parallel read agents (films UI / photos UI / CMS layer /
design system + overlay / new features + undocumented commits) + first-hand verification
of every code-level claim (lint, full chromium E2E run, spot-reads).

Context since last audit (2026-05-21, CMS-only, fixes in `db09110`): PR #29
(other-projects rework), PR #30 (section-title hover + global type scale → docs 15/16),
PR #31 (`test/other-projects-3col` — Other Projects multi-column grid, **merged**),
plus hero fixes (`3d1dec4` drop sticky pin, `d9dc6e4` mute-on-open, `5efbc99`
mobile scroll-before-expand, `b0b0be6` band alignment), doc 17 (CardSubtitle).

**Lint:** clean (zero warnings). **E2E (chromium, 56 tests): 52 pass, 4 fail** — see [TEST] below.

---

## Prior-audit verification — 13/13 findings landed in `db09110`

H1 ✅ upload-films.mjs writes only schema fields incl. `visible` · H2 ✅ doc 10 rewritten
(status SHIPPED, videoMux/visible, removed-fields block) · M1 ✅ webhook-Delay myth gone
from all 3 scripts · M2 ✅ doc 07 overlay model superseded note + 6 anchors · M3 ✅ GROQ
filter has "film" · M4 ✅ seed constants current shape · M5 ✅ watch-cms listens to all
6 types · M6 ✅ client.js comment corrected · M7 ✅(partial) imagePosition scrubbed from
script/loader/doc 09 — **one ghost survives: fetch-cms-content.mjs:115 comment** ·
M8 ✅(partial) doc 12 status SHIPPED + as-built 1080p note — **720p survives at doc 12
L54 + L162** · M9 ✅ heroPhotos `.warning()` · L1–L4 ✅.

---

### [TEST] E2E suite status (run 2026-06-12, chromium)

**Files:** all of tests/, playwright.config.js
**Findings:**
- 🟠 HIGH `cms-films-cdn.spec.js:20` **deterministically red.** Asserts an `<img>` with
  `cdn.sanity.io` src on `/`. Premise broken twice over: featured films now use Mux
  posters (`image.mux.com`, and as `<video poster>`, not `<img>`), and collapsed card
  bodies are unmounted while collapsed (no thumbnail `<img>` at rest). cms.json: 17/17
  films have muxPosterUrl, only 5 have a Sanity thumbnail.
- 🟠 HIGH `cms-studio.spec.js:20` **red, /admin is healthy.** Page snapshot shows the
  Sanity v4 login screen renders fine; the spec's shell selector
  (`[data-ui="Studio"], [data-testid="login"], [data-testid="default-layout"], main`)
  matches nothing in the current login DOM (all generic divs). Selector drift.
- 🟠 HIGH `featured-photo-cards.spec.js:130` (lightbox) **deterministically red.**
  Lightbox became a 3-slide swipe carousel (prev/current/next on a translated track,
  Lightbox.jsx:266–302); the spec's `lightbox.locator('img').first()` grabs the *prev*
  slide at x≈-913. Test drift — the lightbox itself centres correctly (portal to body
  is intact, Lightbox.jsx:354).
- 🟡 MEDIUM `featured-photo-cards.spec.js:201` (anchored switch) **flaky** — failed in
  the full-suite run, passed isolated. Plus 4 page-load timeouts on /photos when run
  alongside other load (passed in full run). Load-sensitivity, not logic.
**Verified false alarms:** `--section-padding-top/bottom` ARE defined (inline custom
props, FeaturedFilmCard.jsx:208-209); `tests/hero-overlay-sizing.spec.js` is a single
file correctly picked up by `testDir: './tests'`.

### [01] Photos Hero Slideshow
**Files read:** doc 01, FloatingGalleryHero.jsx, loader.js, data/heroPhotos.js
**Findings:**
- 🟡 MEDIUM Doc body (Purpose/Architecture L22–28) still presents the pre-CMS model as
  live ("cycles through all 116 photos from public/img/BABA PHOTOS", "photo list lives
  in src/data/heroPhotos.js") — contradicts its own frontmatter known_issue #3 (CMS is
  the source, loader fallback only). CMS holds 115 photos.
- 🟢 Both open known_issues verified still real: advance() returns silently after 20
  consecutive landscape-skips (frozen frame possible, L100–122); prefersReducedMotion
  read once at mount, non-reactive (L64–66, contrast reactive isMobile L69–75).
- 🟢 Shuffle/interval/preload/hard-cut/overlay-mount claims all verified accurate.
**Test coverage:** indirect only (hero visibility in featured-photo-cards spec). No
behavioral test of the slideshow (src changes over time, landscape-skip, reduced-motion).
**Doc accuracy:** frontmatter good; body prose pre-CMS, needs 5-min update.

### [02] Design System
**Files read:** doc 02, index.css, tailwind.config.js, fluidScale.js, siteConfig.js, public/fonts/ (ls)
**Findings:**
- 🟠 MEDIUM known_issue claims fluidScale "mobileRatio 0.4 for hero overlay offsets" and
  HeroSection.jsx as consumer. Offsets retired; no 0.4 path exists (0.85 nav default,
  0.75 OVERLAY_TEXT_MOBILE_RATIO via heroOverlayLayout.js:102); consumer is
  heroOverlayLayout.js, not HeroSection.jsx.
- 🟠 MEDIUM `HeroBioOverlay` documented as exported from HeroSection.jsx in 3 places
  (L96/101/190); actual export is HeroOverlay.jsx:289 since the doc-13 refactor.
- 🟡 LOW Self-contradiction: `--text-hero` = 8.75rem = 157.5px (L86, index.css:158) vs
  "140px" in the historical note (L103) + tailwind.config.js:74-75 comment (pre-18px-root value).
- 🟡 LOW Stale line refs (transitions :200-202 → 203-205; reduced-motion :475 → 511).
- 🟢 Font paths, color tokens, px-pinned layout constants, siteConfig hardcoded-fields
  list all verified accurate.
**Test coverage:** none by design (source-of-truth doc). **Doc accuracy:** good on tokens; 2 workflow-misleading drifts.

### [03] Collapsed Film Cards
**Files read:** doc 03, CollapsedFilmCard.jsx, FeaturedFilmCard.jsx, Films.jsx, tailwind.config.js, SmoothScrollContext.jsx, collapsed-film-cards.spec.js
**Findings:**
- 🟠 MEDIUM "Layout & Spacing — the collapsed band" (L105–111) entirely stale: describes
  the retired 3-tier band (`md:flex-row` 3-zone row, `items-stretch`, `md:min-h-[36px]`,
  `cards:pl-*` drift offsets — zero grep hits). Reality: band is a stacked
  `flex flex-col gap-[3px]` card at every width (CollapsedFilmCard.jsx:386); layout is
  the parent grid `grid-cols-2 md:grid-cols-3 lg:grid-cols-4` (Films.jsx:130) with
  `col-span-*` breakout when expanded (CollapsedFilmCard.jsx:358). Grid never mentioned.
- 🟠 MEDIUM `tailwind.config.js # cards (1350px) breakpoint` in source_files +
  Dependencies — the feature no longer uses the `cards` screen at all.
- 🟡 LOW State-machine "≈36px one-line row" → real height ~50–60px; 36 survives only as
  a vestigial close fallback (L290).
- 🟡 LOW Click-areas table: "Text column — Inert — stopPropagation" → stopPropagation
  removed (FeaturedFilmCard.jsx:266-268); clicking text now bubbles and closes the card.
- 🟢 Known issues #1–#3 verified still accurate (incl. mobile centre-before-shutter,
  satisfied by 5efbc99's path: scroll → body.minHeight pin → shutter, L170–212).
- 🟢 No code bugs: isAnimatingRef re-entry guard, phase-keyed scroll lock, filtered
  transitionend, cancellable mobile scroll, minHeight cleanup all verified.
- 🟡 LOW (latent) resize across 1024px mid-animation can mismatch open technique —
  effectively unreachable (scroll locked, 1200ms window); note as known_issue only.
**Test coverage:** 3 specs green, selectors current. Untested (by design or gap): X-strip
close vs article click, reduced-motion path, keyboard open, grid col-span breakout.
**Doc accuracy:** behavior/state machine excellent; layout half pre-grid.

### [04] Featured Card Fade on Scroll
**Files read:** doc 04, FeaturedFilmCard.jsx, SmoothScrollContext.jsx, homepage.spec.js
**Findings:**
- 🟠 MEDIUM Ramp size self-contradiction: code `FADE_RAMP_VH = 0.20` (L104-105); doc says
  20% in 3 places but "~10%" in Purpose L20 + Verification step 5 L88 (stale earlier value).
- 🟠 MEDIUM "Coexistence with collapsed cards" + Dependencies describe a
  `visibility: hidden` no-op-write mechanism that no longer exists — collapsed bodies are
  **unmounted** (`showContent = phase !== 'collapsed'`, CollapsedFilmCard.jsx:334); the
  fade listener simply isn't registered until expand (5b96fd9 drift).
- 🟡 LOW Architecture pseudo-code folds smootherstep differently than Business Rules; align naming.
- 🟢 Everything else line-for-line accurate: smootherstep polynomial, reduced-motion
  early-return, initial update, resize recompute, cleanup, no-transition, unified
  desktop/mobile listener path. No leaks (unsub + resize removal + opacity reset verified).
**Test coverage:** none for the fade (documented as intentional; homepage.spec.js is
generic smoke). **Doc accuracy:** math/lifecycle accurate; 2 drifts above.

### [05] Featured Photo Cards
**Files read:** doc 05, FeaturedPhotoCard.jsx, PhotoCardPreview.jsx, ExpandedPhotoGallery.jsx, Photos.jsx, loader.js, fetch-cms-content.mjs, photoProjects.js
**Findings:**
- 🟠 HIGH The entire "Anchored Open — Sibling Collapses Invisibly" section (L208–237) +
  the `flushSync`-in-`handleOpen` claim (L206) describe a superseded model. Code:
  `onWillExpand` WITHOUT flushSync (explicit comment L154-159: open + sibling close
  animate in parallel), FIRST rects captured immediately, sibling collapse **deferred**
  to the `photo-card-expand-done` window event (L365–445). Docs 05 and 06 now describe
  the same shared mechanism in two contradictory ways (5b96fd9 unification).
- 🟡 MEDIUM Data Model documents dead `imagePosition` (L175-176) — side is purely
  `index % 2` (FeaturedPhotoCard.jsx:68; Photos.jsx:110 comment confirms).
- 🟡 MEDIUM Constraints L301 "addScrollListener is unused here" contradicts the doc's own
  Header Pin section — ExpandedPhotoGallery.jsx:41,108 uses it for the desktop header pin.
- 🟡 MEDIUM source_files lists scripts/scaffold-photo-projects.mjs "# obsolete" — exists
  but shouldn't be a live source_file.
- 🟡 LOW PATTERNS now 0–15 (11–15 = side-by-side comparison patterns); doc says "10 layouts".
- 🟢 All animation constants verified exact; FLIP choreography (width-only scale, stagger
  clamp, mobile serialize-scroll-then-morph, clip-path) accurate; **mobilePattern contract
  verified intact end-to-end** (schema → GROQ → flatten → preview.* → PhotoCardPreview).
**Test coverage:** 8 specs match current DOM; lightbox spec red (see [TEST]). Gaps:
no cross-type test, no reduced-motion, no mobile-viewport test.
**Doc accuracy:** constants/FLIP accurate; anchored-open narrative is the largest doc
drift in the project.

### [06] Collapsed Photo Cards
**Files read:** doc 06, CollapsedPhotoCard.jsx, ExpandedPhotoGallery.jsx, Photos.jsx, tailwind.config.js, CollapsedFilmCard.jsx (cross-ref)
**Findings:**
- 🟠 HIGH Title ("single-row → gallery shutter"), Purpose, and the 3-tier Layout section
  (L118–126) wholly obsolete: bands are now a CSS grid `grid-cols-2 md:grid-cols-3
  lg:grid-cols-4` (Photos.jsx:166) of stacked `flex flex-col` cards
  (CollapsedPhotoCard.jsx:427, explicit comment L419–426); expanded card breaks out via
  `col-span-2 md:col-span-3 lg:col-span-4` (L398). No `md:flex-row`, no `cards:*`, no
  `md:min-h-[36px]` anywhere.
- 🟠 HIGH "Reference implementation for the collapsed-band pattern, shares the cards
  (1350px) breakpoint" — inverted/false post-unification; component uses no `cards:` classes.
- 🟠 MEDIUM Latent fragility (document as known_issue): the cross-type handoff relies on
  `photo-card-expand-done` + a generic `phase !== 'collapsed'` check while the two card
  types use DIFFERENT phase names (`animating-open/animating-close` vs
  `animating/closing`); plus a hard-coded 36px close fallback (CollapsedPhotoCard.jsx:249,
  CollapsedFilmCard.jsx:290) referencing a row height that no longer exists.
- 🟡 MEDIUM Data Model says band shows "first sentence of description" — the `<p>` is
  hard `hidden` at every width (L433); band shows title + year•category only.
- 🟢 Open/close mechanics, constants, scroll-lock, clip-path, coordinated-handoff
  (flushSync + instant scroll-compensate, L303–357) all verified accurate.
**Test coverage:** 🟠 NONE — zero `[data-collapsed-photo-card]` selectors in any spec;
doc 06's claim that featured-photo-cards.spec.js covers the bands is false. Cross-type
single-expand (headline behavior of docs 05+06) completely untested.
**Doc accuracy:** mechanics accurate; layout half + title + test claim obsolete/false.

### [07] CMS Foundation
**Files read:** doc 07, sanity.config.js, schemas (siteSettings/heroOverlay/showreel/index), structure.js, DeployTool.jsx, client.js, loader.js, siteConfig.js, Studio.jsx, fetch/seed/watch scripts, .env.example, package.json, vercel.json, vercel-build.sh
**Findings:**
- 🟠 MEDIUM PR #30's `display` group (featuredTitleHoverEffect invert/grow,
  featuredTitleHoverWholeSection — siteSettings.js:147-172, loader fallbacks L46-48) is
  absent from doc 07's siteSettings table; "5 tabs" is now 6 groups.
- 🟠 HIGH `package.json` `"vercel-build": "vite build"` skips the CMS fetch; real path is
  vercel.json buildCommand → vercel-build.sh (which fetches). Divergence = stale-cms.json
  footgun (carried from last audit's "noted" list, still unaddressed).
- 🟡 MEDIUM Studio bundle size cited 3 ways: doc 07 "~5MB/1.7MB gz", Studio.jsx:8
  "~6MB/1.95MB gz (measured 2026-05-21)", CLAUDE.md "~5MB gz 1.7MB".
- 🟡 LOW siteConfig.js header still "Stage 1 of the CMS migration".
- 🟡 LOW Frontmatter models omits heroPhotos singleton (owned by doc 08 — note only).
**Test coverage:** cms-studio.spec.js — 1 of 2 red on selector drift (see [TEST]).
**Doc accuracy:** good post-db09110 except the display group.

### [08] Hero Photos
**Files read:** doc 08, heroPhotos schema, upload-hero-photos.mjs, fetch script, loader, cms-photos-cdn.spec.js
**Findings:**
- 🟡 LOW "~50 photos" in doc 08 + CLAUDE.md; CMS holds 115.
- 🟢 Validation `.warning()` consistent; Studio label matches structure.js; CDN spec green.
**Test coverage:** adequate (CDN pipeline asserted). **Doc accuracy:** accurate.

### [09] Photo Projects
**Files read:** doc 09, photoProject schema, upload/fix-ranks scripts, fetch script, loader, cms-photos-cdn.spec.js
**Findings:**
- 🟡 MEDIUM fetch-cms-content.mjs:115 JSDoc still lists `imagePosition?` (M7 leftover);
  function never maps it.
- 🟢 Script writes schema-only fields; featured field correctly retained on photoProject;
  LexoRank + deploy-workflow known_issues accurate.
**Test coverage:** adequate. **Doc accuracy:** accurate.

### [10] Films
**Files read:** doc 10, film schema, upload-films/fix-film-ranks, fetch script, loader, films.js, cms-films-cdn.spec.js, CLAUDE.md
**Findings:**
- 🟠 MEDIUM Build-plan step 2 (L177) "passes videoFile through unchanged" contradicts the
  as-built Mux-only flattenFilm; Architecture diagram (L61) omits getNonCollapsedFilms.
- 🟡 LOW films.js header stale ("Every film is currently set to featured"); legacy entries
  still carry dropped fields (tolerated as fallback, header misleads).
- 🟢 upload-films schema-aligned (H1 verified); aspect-ratio resolution chain verified
  against cms.json (override + Mux-probe cases).
**Test coverage:** 🟠 cms-films-cdn test 1 red (see [TEST]). **Doc accuracy:** Data
Model/Business Rules good; historical build-plan misleads.

### [11] Video Uploads (research)
**Findings:** 🟢 Correctly frozen as a decision record (status RESEARCH, source_files []).
No action.

### [12] Mux Video
**Files read:** doc 12, sanity.config.js (muxInput), film/showreel schemas, migrate-blob-to-mux.mjs, fetch script, loader, cms-films-mux.spec.js, cms.json
**Findings:**
- 🟠 MEDIUM source_files lists phantom `src/components/films/FilmCard.jsx` (+ build-plan
  step 7 prose). No such file; poster logic lives in Featured/CollapsedFilmCard.
- 🟡 MEDIUM Loader-projection section documents `streamUrl`/`posterUrl`; shipped names are
  `muxStreamUrl`/`muxPosterUrl` (+`?time=0.5`) (fetch script L210-213, cms.json).
- 🟡 LOW 720p residue at L54 + L162 (M8 partial; as-built note says 1080p).
- 🟢 Migration script matches muxInput config (1080p/smart/public/mp4 none), idempotent;
  showreel Mux path verified end-to-end; .env.example omission is by design.
**Test coverage:** mux spec green. **Doc accuracy:** good except above.

### [13] Hero Overlay Mobile
**Files read:** doc 13, heroOverlayLayout.js, HeroOverlay.jsx, heroOverlay schema, HeroSection.jsx, FloatingGalleryHero.jsx, loader.js
**Findings:**
- 🟠 MEDIUM Business Rules `top: max(96px, <offset>)` nav-clamp paragraph (L136-138) is
  dead — code is a flat `9rem` (144px) inset, no offsets (heroOverlayLayout.js:120,133).
  Reads as live despite the supersession banner.
- 🟡 LOW `MOBILE_ZONE_GAP_PX` (16, used HeroOverlay.jsx:234,253) undocumented.
- 🟢 Every other constant verified: 104 nav-safe, 16/32 pads, 0.6 floor, offsetHeight
  measurement (no transform feedback loop), pointer-events model, size scale + initialValue,
  legacy fallback, 0.75 ratio, twice-mounted variant pattern on both heroes.
**Test coverage:** strong (nav overlap, zones, containment, sizing on / and /photos).
**Doc accuracy:** strong; one dead paragraph.

### [14] Hero Overlay Sizing
**Files read:** doc 14, heroOverlayLayout.js, HeroOverlay.jsx, schema, hero-overlay-sizing.spec.js, playwright.config.js
**Findings:**
- 🟢 Fully accurate — tier boundaries (768/1023/1024 verified at edges), size resolution
  (auto/manual per tier), fixed insets (40/144/40px, middle centred), stack gap 0.55,
  schema field split (Basics/Advanced/hidden) all match code + unit tests.
**Test coverage:** excellent (pure-logic spec + E2E split as documented). **Doc accuracy:** highest in project.

### [15] Section Title Hover Zones
**Files read:** doc 15, FeaturedFilmCard.jsx, FeaturedPhotoCard.jsx, siteSettings schema, loader.js, section-title-hover.spec.js
**Findings:**
- 🟡 LOW Terminology flips between "Highlight" (user-facing) and `invert` (value); schema
  title is "Highlight (black background)". Pick one + note value once.
- 🟢 Hover-zone bubbling + relatedTarget guard, grow-on-h3 / invert-on-span placement,
  photo-card phase reset, whole-section click + stopPropagation all verified exact.
**Test coverage:** good (7 specs, both pages); toggle-off CSS :hover path manual-only (documented stance).
**Doc accuracy:** high.

### [16] Global Type Scale
**Files read:** doc 16, index.css, heroOverlayLayout.js, TitleSection.jsx, tailwind.config.js
**Findings:**
- 🟠 MEDIUM TitleSection.jsx:14 is `text-[34.25px] md:text-[38.25px]` — doc 16 documents
  only the desktop 38.25 (34×1.125) and omits the mobile 34.25 which breaks the
  "everything ×1.125" narrative; component comment doesn't explain it either.
- 🟢 Root 18px, pinned-px list verified COMPLETE (no rem max-widths missed), TYPE_SCALE
  1.125 in heroOverlayLayout, nav px-immunity, unitless line-heights all verified.
**Test coverage:** none by design. **Doc accuracy:** high; one omission.

### [17] Card Subtitle Separators
**Files read:** doc 17, CardSubtitle.jsx + repo-wide import grep, all 5 consumers
**Findings:**
- 🟠 MEDIUM Zero tests for a pure, easily-testable function (test_files: [] is honest but
  the 8 fill-combinations + trim/number edge cases would regress silently — e.g. a
  "simplified" truthy filter would drop year 0).
- 🟢 All 5 consumers verified real importers (FilmModal correctly not one); separator
  styles, parts arrays, trim/null/empty logic all verified exact, 0/1-part cases correct.
**Test coverage:** none. **Doc accuracy:** accurate.

### [B] Undocumented changes + CLAUDE.md
**Commits checked:** 3d1dec4, d9dc6e4, 5efbc99, b0b0be6
**Findings:**
- 🟠 HIGH CLAUDE.md Architecture tree: `films/ # FilmCard, FeaturedFilmCard, FilmModal`
  and `photos/ # ProjectCard, PhotoGrid, FloatingGalleryHero` — FilmCard, ProjectCard,
  PhotoGrid DO NOT EXIST; CollapsedFilmCard, FeaturedPhotoCard, CollapsedPhotoCard,
  PhotoCardPreview, ExpandedPhotoGallery missing from the list.
- 🟠 HIGH CLAUDE.md Gotcha "Photo images must be in public/photos/" — photos are uploaded
  in Studio and served from cdn.sanity.io (asset->url, fetch script L76); legacy fallback only.
- 🟠 HIGH CLAUDE.md Key Files "Fullscreen sticky video hero" — sticky dropped in 3d1dec4;
  HeroSection.jsx:150-156 comments explicitly document the non-sticky layout.
- 🟡 MEDIUM d9dc6e4 mute-hero-on-modal-open (HeroSection.jsx:114-120 + Films.jsx:69-77
  `muteHero()`) documented nowhere.
- 🟡 LOW CLAUDE.md Gotcha hero video filename: code prefers Mux, fallback is
  `/videos/Showreel 2021.mp4`, not hero-teaser.mp4.
- 🟢 5efbc99 already documented (doc 03); "film detail uses modal" gotcha still true.

### Quality gates (global CLAUDE.md)
- Files >300 lines: FeaturedPhotoCard 608, CollapsedPhotoCard 465, CollapsedFilmCard 460,
  Lightbox 357, FeaturedFilmCard 304. Pre-existing; heavily-commented animation
  orchestrators. Noted, not flagged for mechanical splitting.
